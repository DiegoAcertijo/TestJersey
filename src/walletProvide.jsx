// WalletProvider desactivado (UI ONLY)
// Este archivo existe solo para evitar errores de import

const WalletProvider = ({ children }) => {
  return <>{children}</>
}

export default WalletProvider
